1. To get the bot running do pip install requests in the folders directory.
2. Depending on how many you want to generate you will need to edit the number in config.json, keep it to something reasonable or else you will be ratelimited.